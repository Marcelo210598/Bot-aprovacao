# Snapshot - 2026-06-17

## 🎯 O que foi feito hoje

### Forward test ao vivo (Sim101) — primeira sessão real
- Rodou o bot ao vivo pela primeira vez (não replay) no Sim101
- Sessão diurna: múltiplos trades na mínima 30403,25 (PDL)
- Sessão noturna: NOT_L9, NOT_L10 funcionando

### Bugs críticos encontrados e corrigidos (3 bugs)

**Bug 1 — Trailing sumia no restart (CRÍTICO)**
- Sintoma: bot reiniciou com posição aberta → trailing some → trade foi de +$131,50 para -$20 → fechado manualmente
- Causa: `StartBehavior.WaitUntilFlat` ignora posição aberta no restart, zerando todo o estado
- Fix: `StartBehavior.AdoptAccountPosition` + handler `State.Realtime` que detecta posição aberta e inicializa recovery (trailing assume no 1º tick via `OnMarketData`)

**Bug 2 — Bot se auto-desabilitava (CRÍTICO)**
- Sintoma: `Desabilitando estratégia NinjaScript` aparecia no log; bot parava sozinho após 4 restarts
- Causa: `NullReferenceException` em `TradesHoje()` (`trades[i].Exit.Time` quando trade ainda aberto não tem Exit) e na cadeia `SystemPerformance.AllTrades.TradesPerformance.Currency.CumProfit`
- Fix: null-safety com `?.` e `try/catch` em `TradesHoje()` e `RealizadoAcumulado()`

**Bug 3 — SHORT fantasma pós-exit (race condition)**
- Sintoma: `[TraderOS] Trade: MNQ SHORT x5 PnL=$-34,5` após cada TrailingTick exit
- Causa: server stop (SetStopLoss no nível BE) continuava ativo no servidor depois que `ExitLong` fechava a posição; se preço continuava caindo tocava o stop e abria SHORT x5 fantasma
- Fix: antes de cada `FechaPosicao` em `OnMarketData` (AlvoTick e TrailingTick/StopTick), neutraliza o server stop movendo 5000 ticks: `SetStopLoss(sinalAtivo, CalculationMode.Ticks, 5000, false)`

**Bug extra — Erro de compilação CS7036**
- Sintoma: `SetStopLoss(CalculationMode.Price, ..., false)` sem signal não existe nessa versão do NT8
- Fix: removidos os `else` branches que usavam esse overload; em modo RECOVERY o server stop anterior ainda protege, o trailing sintético gerencia a saída

## 📝 Decisões técnicas importantes

- **AdoptAccountPosition**: a estratégia agora ADOTA posições abertas no restart, em vez de ignorá-las
- **RECOVERY mode**: quando reinicia com posição aberta, `sinalAtivo = "RECOVERY"` e `gerenciando = false` — OnMarketData inicializa tudo na primeira tick a partir do preço atual
- **FechaPosicao com recovery**: usa `ExitLong()` sem signal (fecha tudo) em vez de tentar fechar signal que não existe mais
- **Trailing 1,75pt confirmado ao vivo**: funcionou corretamente — BE acionou, trailing seguiu o preço tick a tick (NIV_L32 e NIV_L33 validados)

## 📊 Análise dos trades da sessão

| Trade | Entrada | Fav | Exit mode | Lucro estimado |
|-------|---------|-----|-----------|----------------|
| NOT_L9 | 30781,50 | 30789,25 | Trailing (bar) | +$32,50 |
| NOT_L10 | 30777,00 | 30802,75 | Trailing (bar) | +$295,00 |
| NIV_L23 | 30411,00 | 30428,75 | Trailing (bar) | +$330,00 ⚠️ |
| NIV_L31 | 30412,50 | 30425,00 | Trailing (bar) | +$257,50 ⚠️ |
| NIV_L32 | 30408,25 | 30415,50 | TrailingTick | ~+$45 |
| NIV_L33 | 30416,25 | 30423,50 | TrailingTick | ~+$55 |
| Phantom SHORT | — | — | — | -$34,50 (bug, corrigido) |
| Trade manual pós-restart | — | peak +$131,50 | Manual | ~-$20 (bug, corrigido) |

⚠️ Saídas via OnBarClose no simulador preenchem no fechamento da barra, não no nível real do trailing. Em conta real os valores serão menores.

**Checkpoint P&L confirmado no NT8: ~$397,50 (screenshot)**

## ⚠️ Problemas encontrados e soluções

- **`~saida` enganoso no log**: para TrailingTick (OnMarketData), `Close[0]` é o fechamento da última barra completa, não o preço real de execução. O preço real está na linha `[STOP INTRABAR]`. Comportamento cosmético — não impacta execução.
- **Slippage em mercado rápido**: TrailingTick disparou em 30412,75 (NIV_L32) mas fill pode ter sido em 30408,25 (mercado caindo fast). A monitor em conta real.

## 🔧 Configurações/código alterados

Commits de hoje (branch `feat/estrategia-noturna`):
- `c0efd1d` — fix restart/disable (AdoptAccountPosition + null-safety)
- `7ba2017` — fix server stop race condition (neutralize 5000 ticks)
- `aae7e8d` — fix CS7036 (remove overload SetStopLoss sem signal)

## 📊 Estado atual do projeto
- 100% bugs críticos do forward test corrigidos
- Código atualizado no GitHub (`Marcelo210598/Bot-aprovacao`, branch `feat/estrategia-noturna`)
- NT8 precisa recompilar com o arquivo atualizado (usuário fazendo manualmente)
- P&L da sessão: ~$397,50 no Sim101

## 🚧 Próxima sessão (To-do)
- Confirmar que o código novo compilou sem erros no NT8 ✅ (pendente confirmação)
- Continuar forward test ao vivo no Sim101 com o código corrigido
- Monitorar se o SHORT fantasma sumiu (bug 3 corrigido)
- Monitorar se o bot sobrevive a restarts sem perder o trailing (bug 1 corrigido)
- Avaliar merge da branch `feat/estrategia-noturna` para `main` quando forward test validar

## 💡 Observações importantes
- O bot está operando em Sim101 (simulação), não em conta real ainda
- Bugs corrigidos hoje foram descobertos no PRIMEIRO DIA de teste ao vivo — normal e esperado
- A estratégia noturna (NOT_L9, NOT_L10) gerou os melhores lucros da sessão
- Trailing 1,75pt é MUITO apertado — considera aumentar para 2,5pt-3pt em backtest antes de ir ao vivo

---

# 🌙 SESSÃO DA NOITE (17/06) — evolução da NOTURNA + fuso-proof + replay

## 🎯 O que foi feito
1. **Gatilho 'corpo' (j2)** — substituiu pavio+rejeição. Vela A toca a zona (sem rejeição) → arma no
   corpo de A → vela B/C rompe o corpo → entra. Backtest: COMBINADO 100% (27/27), PF 1.63, $60,9k/ano,
   OOS 100/100. (`run_noturna_corpo.py`). Commit `2ca5fe7`.
2. **Bot FUSO-PROOF** — `ResolveFusoGrafico()` lê por reflection o fuso de exibição do NT; `EmET()`/`EmBR()`
   convertem diurna→ET, noturna→BR em QUALQUER fuso de gráfico. Fallback: assume ET (comportamento antigo).
   Corrigiu a janela noturna que fechava 1h cedo quando o gráfico estava em horário BR. Confirmado no
   replay: log "grafico 19:01 = BR 19:01" (antes somava +1h errado). Commit `2ca5fe7`.
3. **Noturna 5min (revertido)** — testado e implementado, depois REVERTIDO pro 1min (1min rende mais:
   $60,9k vs $46k do misto). Commit `deb0e6b` (5min), `486d4e4` (volta 1min).
4. **Entrada TICK A TICK** — a noturna agora entra no `OnMarketData` no instante que o preço cruza o
   corpo de A (vela B/C), SEM esperar a vela fechar. Rede p/ backtest histórico (State!=Realtime) dispara
   no fechamento. Faz o ao vivo bater com o backtest (fill no nível). Commit `486d4e4`.
5. **StartBehavior** — `AdoptAccountPosition` não existe no dropdown do NT (só por código). Manual: usar
   `WaitUntilFlatSynchronizeAccount` (sincroniza conta, RECOVERY funciona) OU remover+readicionar a
   estratégia pra pegar o SetDefaults.

## 🧪 Backtests rodados (TODOS rejeitaram "melhorias" intuitivas — baseline é o ótimo)
- **corpo+pavio** (romper a vela inteira): PIOR ($46k→$42k, noite PF 1.34→1.21). `run_noturna_extremo.py`
- **filtro anti-tendência** (não fadar extremo novo, N=1..5 barras estável): PIOR ($48-57k). `run_noturna_filtro_tendencia.py`
- **zona apertada** (5-15% em vez de 23,6%): PIOR ($48-55k). `run_noturna_zona.py`
- **5min vs 1min**: 1min melhor. `run_noturna_5min.py`
- **níveis à noite** (fade PDH/PDL em vez de Fib): PIOR (mal opera, ~$1k). `run_niveis_noite.py`
- **fecha-além-do-close** (Close[0]<Close[1]): PIOR ($50k). `run_noturna_1min_rev.py`
- **TOQUE FRESCO** (re-arma só após preço sair da zona e voltar a tocar): mesmo PF (1.58), mas $60,9k→$44,1k.
  Único que mantém o PF — é ESCOLHA DE ESTILO (menos trades/mais limpo vs mais $$). `run_noturna_retouch.py`.
  ⏳ PENDENTE decisão do Marcelo: implementar ou não.

## 📊 Replay 12-16/06 (rodando 22h) — noite 15/06 (downtrend forte)
- Entrada tick + BE + trailing confirmados funcionando. Sem SHORT fantasma, sem auto-disable.
- Fuso-proof confirmado (grafico=BR detectado, janela 19-21h no horário certo).
- Warm-up 19:15 confirmado (não opera 19:00-19:15, só forma o canal).
- 15/06 foi noite de DOWNTREND: reversão comprou fundos caindo → 3 stops cheios (~-$125 cada),
  recuperou com bounces. Net da noite ~-$178 (capado pelo stop diário $750). Replay "fura" os fills
  (encolhe ganhadores) → ao vivo no Sim101 seria menos negativo.

## 🔑 LIÇÃO-CHAVE da sessão
**Toda restrição de seletividade na entrada da reversão REDUZ o PnL.** Os trades "no meio do canal" que
parecem ruins num downtrend são os MESMOS que pegam bounces vencedores em outras noites. Não dá pra
separar perdedor de ganhador (mesmo setup). A proteção contra noite ruim é o **STOP DIÁRIO $750**, não
filtro de entrada. O baseline (1min, corpo, zona 23,6%, j2, sem filtro) é o ótimo comprovado.

---

# 🌙 PARTE 2 (madrugada 18/06) — GATILHO FINAL: na LINHA (extremo), não nas Fib centrais

## A virada de entendimento (depois de MUITOS prints do Marcelo)
- Tentei "TOQUE FRESCO" (re-armar só após preço sair da zona e voltar) → backtest $44k, mesmo PF.
  Implementei como toggle. **MAS bloqueou entradas válidas** (reset "preço acima da linha" não disparava
  num downtrend) → ERRADO. Toggle `ToqueFresco` ficou **default OFF**.
- **ESCLARECIMENTO-CHAVE do Marcelo:** "a linha" da estratégia é o **EXTREMO do canal (0% Min / 100% Max)**,
  NÃO as Fib centrais (23,6%/76,4%). Eu (e o código) usava as Fib o tempo todo → entradas no "meio".
  A NOT_L24 (entrou em 30787, longe do fundo 30755) era o exemplo: tocou a zona Fib, NÃO a linha.

## Implementação FINAL (commit `6ea8471`)
- Gatilho: `zVenda = noiteHigh - LinhaToleranciaPontos`; `zCompra = noiteLow + LinhaToleranciaPontos`.
  Vela A toca/passa a LINHA (extremo) dentro da tolerância → arma no CORPO de A → vela B passa o corpo
  → entra TICK A TICK. Removidas FIB_VENDA/FIB_COMPRA (órfãs).
- Parâmetro **`LinhaToleranciaPontos`** (default **5pt**), tunável no grupo "7. Noturna".
- Desenho do canal: destaca LINHA de alta/baixa (sólida) + banda de tolerância (dourada). Sumiram as
  Fib centrais que confundiam. Painel: "LINHA de ALTA (vende)" / "LINHA de BAIXA (compra)".
- Backtest `run_noturna_linha.py`: **+5pt → 100% (26/26), OOS 100/100, ~$48k/ano**. (10pt $52k, 15pt $55k).

## Replay 15/06 com o gatilho na linha (downtrend)
- L30 +$18,5 (tocou linha 30768, bounce) | L31 −$124,5 | L32 −$137 | L33 +$53 → **net −$190**.
- ✅ Entradas confirmadas NA LINHA (não no meio) — Marcelo aprovou a estética/lógica.
- 🔴 Mesmo na linha, downtrend = stop quando a linha ROMPE (faca caindo). Nenhuma regra de entrada
  conserta isso — confirmado de novo. Proteção = stop diário $750.

## 🔑 LIÇÃO FINAL (reforço)
Marcelo iterou MUITO no gatilho. Resultado: o gatilho na LINHA (extremo) é o que ele quer (entradas
limpas, na linha), custa ~$48k vs $60k da Fib, mas é a ESTRATÉGIA dele e é robusto (100%/OOS). Downtrend
sempre vai sangrar (capado pelo stop diário). Não propor mais filtros de entrada — todos pioram.

## 🚀 PLANO DE DEPLOY (definido pelo Marcelo)
- **Andersson testa quinta (18/06) e sexta (19/06)** — Marcelo vai enviar o arquivo do bot.
- **Segunda (22/06): início do teste na CONTA REAL** tentando aprovar (Apex).
- ⚠️ Antes do real: trocar **StartBehavior → AdoptAccountPosition** (ou WaitUntilFlatSynchronizeAccount).
- Config atual: gatilho na linha, tol 5pt, ToqueFresco OFF, corpo+tick j2.

## 🚧 Pendências
- Andersson: exportar ZIP do NT8 e enviar (File → Export → NinjaScript). Importar via File → Import.
- Avaliar merge `feat/estrategia-noturna` → `main` após validação do Andersson.
- Forward test Sim101 ao vivo (noite) antes do real.
