# Snapshot — 20/08/2026

## 🎯 O que foi feito hoje (sessão longa — auditoria + Market Replay + pesquisa de estratégia)

### 1. Auditoria de código (Fase 1) do experimento de saída parcial + BE-lock
- Diff completo `BotAprovacao_BASELINE_BACKUP.cs` vs `BotAprovacao_SaidaParcial.cs`, linha a
  linha. Confirmado: produção (`BotAprovacao.cs`) e backup continuam byte-idênticos.
- **Achado crítico:** a saída parcial (4+1@20pt) só existe em `OnMarketData` (tick a tick, só
  roda em Replay/live) — **não existe** em `GerenciaPosicao` (motor de barra, usado em backtest
  e como rede em realtime). Nunca disparou em ~20 dias de Replay real até hoje.
- Possível interação não confirmada: saída parcial pode contar como 2 "trades" pro
  `TradesHoje()`/`MaxTradesDia=12` (não testável sem um trade real disparando a parcial).

### 2. Preparação pro Market Replay A/B controlado
- 3 arquivos novos, todos cópias isoladas, zero lógica de entrada/saída alterada:
  `BotAprovacao_BASELINE_REPLAY.cs`, `BotAprovacao_SaidaParcial_REPLAY.cs`,
  `BotAprovacao_SaidaParcial_REPLAY_DEBUG.cs` (com logging extra: TRADE OPEN, BE ativado,
  execução real via `OnExecutionUpdate`, heartbeat por barra, MFE/MAE).
- Achado e corrigido: os 3 (+ o `BotAprovacao_SaidaParcial.cs` original) gravavam o PnL diário
  no MESMO arquivo que a produção — corrigido, cada um agora tem caminho próprio.
- Guia completo em `docs/market-replay-guia-comparacao.md`.

### 3. Registro dos trades 19-30/06 (Market Replay) + correção de comparação
- Registrados via print: 22, 24, 25, 26, 29/06 (19, 23, 30 sem entradas).
- **Erro meu corrigido:** comparei com o baseline errado (pasta de 5min) na primeira tentativa.
  O baseline certo (`2026-06-1min/`) já existia — bati os dois e confirmei Δ=0 em todos os dias
  desse bloco (mecanismo nunca disparou). **29/06 ficou incompleto** (faltam os 2 últimos trades
  do dia, baseline tem 7, só temos prints de 5) — ainda pendente.
- Δ confirmado até 28/06: baseline +$58,5 → nova versão +$181,0 → **+$122,5** (tudo concentrado
  no bloco 01-18/06, zero no bloco 19-28/06).

### 4. Gestão de saída — testes de "subir ganho" e "diminuir perda" (todos REJEITADOS)
- **Alvo da parcial menor que 20pt** (5/8/10/12,5/15pt): piora quanto menor o alvo. Mesmo o 20pt
  original fica abaixo do "só BE-lock" no motor de barra completo.
- **Corte de perda antecipado** (2 de 5 contratos se for contra sem nunca ter favorecido): piora
  — corta futuros vencedores junto (10,7% dos vencedores tocam MAE profundo antes de reverter).
- **Filtro de entrada por `dist_nivel`/sexta-feira**: como corte, estoura o prazo de 30 dias
  (mediana de 31-85 dias pra aprovar); como sizing seletivo, `dist_nivel` piora muito (comum
  demais), sexta fica neutro-modesto (não é achado forte).
- Tudo registrado em `docs/melhorias-sugeridas.md` itens #14 (BE-lock, validado), #15 (parcial
  menor, rejeitado), #16 (loss-cut, rejeitado).

### 5. Comparativo de estratégias novas (ORB / EMA+VWAP+RSI / "ICT-lite" FVG)
- 4 estratégias sozinhas + 10 combinações, mesmo motor de aprovação (DD real $1.000,
  MaxTradesDia=12, slippage 2 ticks). **Nenhuma supera a reversão sozinha.** Mesclar com a
  reversão sempre piora ela (MaxTradesDia compartilhado "engole" o espaço dela).
- Script: `backtest/run_estrategias_comparativo.py`. Doc: `docs/comparativo-estrategias-20-08.md`.

### 6. Taxa de aprovação REAL — 3 metodologias testadas
- Ciclo infinito (70%, otimista — sem limite de dias) → mês calendário (15%, pessimista —
  desperdiça dias após estouro cedo) → **janela de 30 dias rolante com retry imediato (50%, a
  correta)**.
- **BOT 1 vs BOT 2 no teste correto:** BE-lock 0,75 sozinho = taxa **idêntica** ao bot 1 (50%,
  24 tentativas, 12 aprovações — número igual, não é contradição do Δ real de Replay, só é
  pequeno demais pra virar tentativa inteira nesse dataset). Saída parcial completa piora de
  novo (50%→40%).
- Doc: `docs/taxa-aprovacao-30dias-20-08.md`.

### 7. Fix aplicado: `UsarSaidaParcial=false` por padrão
- Nos 3 arquivos experimentais (`SaidaParcial.cs`, `_REPLAY.cs`, `_REPLAY_DEBUG.cs`). BE-lock
  0,75 continua ligado. Produção e backup intocados (confirmado por diff antes e depois).

## 📝 Decisões técnicas importantes
- **Taxa de aprovação real da melhor config: 50%** (não os 70% usados até ontem) — é o número
  pra planejar o negócio daqui pra frente.
- Config recomendada: **BOT 2 (`BotAprovacao_SaidaParcial.cs`) com BE-lock 0,75 ligado e saída
  parcial DESLIGADA** — única combinação que bate ou empata o baseline em todos os testes de
  hoje.
- Nenhuma estratégia alternativa (ORB/EMAV/ICT) testada vale a pena, sozinha ou mesclada.

## ⚠️ Problemas encontrados e soluções
- Baseline errado usado na primeira comparação do bloco 19-30/06 → corrigido usando a pasta certa.
- Colisão de arquivo de PnL diário entre estratégias que rodariam juntas no Replay → cada uma
  agora grava num arquivo próprio.
- 2 trades de 29/06 ainda faltando pra fechar o mês de junho por completo.

## 🚧 PRÓXIMA SESSÃO (AMANHÃ) — Marcelo precisa alterar o bot no NT8

**Qual bot alterar:** `BotAprovacao_SaidaParcial.cs` — **o SEGUNDO bot** (o experimental, que já
está rodando no gráfico do Market Replay). O primeiro (`BotAprovacao.cs`, produção) e o backup
**não mudam, nunca**.

**O que fazer nele, passo a passo:**
1. Recompilar no NinjaScript Editor (`F5`) — já peguei a mudança de código (`UsarSaidaParcial`
   agora tem default `false`).
2. **Passo manual único e obrigatório:** na instância que já está no gráfico, abrir as
   propriedades da estratégia e conferir se `[EXP] Usar saida parcial` está em `false`. O
   recompile NÃO atualiza sozinho o valor de uma instância já adicionada ao gráfico — só um
   `false` clicado à mão (ou remover e readicionar a estratégia) resolve isso.
3. Confirmar que `[EXP2] Usar BE-lock proporcional` continua `true` e `[EXP2] Fração do MFE`
   continua `0,75` (esses NÃO mudam).
4. Seguir rodando o Replay normal, dia a dia, do jeito que já vinha fazendo.

**Depois disso:** continuar os testes e refinamentos incrementais — cada situação que o Marcelo
notar no dia a dia (tipo os "deixou dinheiro na mesa" de 24/06 e 25/06), sinalizar e ajustar aos
poucos, sem nova varredura grande por enquanto. Pendência secundária: os 2 trades que faltam de
29/06 pra fechar o mês de junho.

## 💡 Observações importantes
- O teto de 50% parece estrutural (testado exaustivamente hoje: gestão de saída, sizing, filtro
  de entrada, 3 famílias de estratégia nova — nada melhora). Vale, em algum momento, decidir se
  50% é um número com que dá pra tocar o negócio (rodando várias avaliações em paralelo) ou se
  compensa investir numa estratégia genuinamente nova do zero.
- Todos os scripts novos de backtest estão em `backtest/`: `run_qualidade_entrada_sweep.py`,
  `run_sizing_seletivo_sweep.py`, `run_gestao_saida_sweep.py`, `run_estrategias_comparativo.py`,
  `run_mes_a_mes.py`, `run_janela_30d.py`. Todos rodam a partir da raiz do projeto
  (`python3 backtest/nome.py`), pois `NQ_dados/` fica na raiz.
