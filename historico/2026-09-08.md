# Snapshot — 08/09/2026

## 🎯 O que foi feito hoje

**MILESTONE: Fase A do ONFADE APROVADA — equivalência lógica Python ↔ NinjaScript comprovada.**

### 1. Import da série contínua MNQ pro NT8 (resolvido depois de tropeços)
- Objetivo: ter no NT8 Analyzer o MESMO dado que o `onfade_harness.py` (Python) usa
  (Databento GLBX, front-month contínuo, convertido pra **Eastern**).
- Arquivo gerado por `backtest/gera_import_nt8_mnq.py` → `MNQ_NT8_import_2022_2026_CONT.zip`
  (nome interno `MNQ 12-21.txt`, ~1,5M barras, 2022-05 → 2026-08).
- **Problemas encontrados e resolvidos:**
  - Import 1: entrou num instrumento LIXO `MNQDEC2021` (sem espaço) com specs genéricas
    (TickSize 0.01 / PointValue 1) → 0 barras no backtest. Era resíduo de import antigo.
  - Correto: o dado do `MNQ 12-21.txt` caiu em **`MNQ DEC21`** (com espaço) — contrato real
    que o NT8 conhece, specs certas (0.25 / $2), cobertura 2022→2026. Bastou apontar o
    Analyzer pra `MNQ DEC21`.
  - Fuso: o `Ferramentas > Opções > Geral > Fuso horário` já estava em `(UTC-05:00) Hora
    Oriental`. O `TimeZoneInfo.Local` que o `.cs` imprime = relógio do Windows (Brasília) e
    **não** é o que importa — o que importa é `Time[0]`, e o dump confirmou que vem em ET.

### 2. Fase A rodada 1 — reconciliação de barras (`ReconDumpOnly = true`)
- `OnFadeHarness.cs` no Strategy Analyzer: MNQ DEC21, 13/09/2024→01/11/2024, CME US Index
  Futures ETH, `BarLabelOffsetMin = -1`.
- Gerou `timestamps_dump_nt8.csv`. Comparado contra `timestamps_dump.csv` do Python.
- **77/77 barras-chave idênticas** (OHLC + volume) nos 10 dias-amostra.
- Confirma: convenção de rótulo (`label −1min` = início do intervalo = `ts_et` do Python) e
  que `Time[0]` está em ET. `BarLabelOffsetMin = -1` correto.

### 3. Fase A rodada 2 — reconciliação de trades (`ReconDumpOnly = false`, `SlipTicks = 0`)
- Janela de trade 2024-09-17 → 2024-10-31. Gerou `trades_ninjascript_slip0.csv` (38 trades).
- Comparado campo-a-campo contra `trades_python_slip0.csv` (38 trades).
- **38/38 trades idênticos:**
  - preços de entrada teórico/efetivo — idênticos
  - stop teórico / alvo teórico — idênticos
  - timestamps de sinal / entrada / saída — idênticos
  - motivo de saída (ALVO/STOP) — idêntico
  - PnL bruto e líquido — idênticos
  - **total líquido −US$ 134,72 nos dois lados**; **6 wins / 38**
  - único diff: `contract` = `MNQ` (NT8) vs `MNQZ4` (Python) → **cosmético**, nome do
    instrumento, zero efeito na lógica.

### 4. Manutenção
- `OnFadeHarness.cs`: guard no `FlushFiles()` — se `totalBars == 0`, imprime diagnóstico
  claro (instrumento sem dado / specs erradas / fuso) em vez de crashar com "Index out of
  range". Cosmético (só afetava instâncias vazias do Analyzer). **Falta recompilar no NT8.**
- `src/OnFadeNative.cs` — **NOVO**. Fase B (execução nativa). Ver seção abaixo.
- Evidências salvas em `backtest/onfade_faseA/` (os 4 CSVs comparados).

## 📝 Decisões técnicas

- **Contrato expirado como slot de import:** usar `MNQ DEC21` (5 anos vencido) garante que o
  NT8 não mistura feed Rithmic no backtest. Slots ativos (`MNQ 12-25`, `MNQ 12-23`) sofriam
  override do feed fino real. `MNQ DEC21` tem specs corretas porque o NT8 mantém definição de
  contratos MNQ até ~DEC19.
- **Transferência de arquivo Windows→Mac:** o Marcelo colou o CSV no Apple Notes (iCloud);
  daqui dá pra ler direto do `NoteStore.sqlite` (blob gzip). Fluxo sem risco de travar o
  terminal com paste gigante. Usar sempre pra arquivos grandes vindos do NT8.
- **`OnFadeNative.cs` NÃO calcula comissão nem slippage** — vêm do Strategy Analyzer
  (template de comissão + campo "Deslizamento"). O CSV nativo registra só fill BRUTO, pra
  conferir mecânica. Veredito econômico = relatório do Analyzer.

## ⚠️ Limitações / o que NÃO concluir

- O recorte da Fase A (17/09→31/10/2024, 6 semanas, 38 trades, −US$ 134,72) **NÃO é resultado
  de rentabilidade.** Fase A só provou que o `.cs` reproduz o `.py` bit a bit. Amostra curta
  demais pra qualquer conclusão econômica.
- Comissão `1.44` ainda é **PROVISÓRIA** — confirmar o valor real (Rithmic/Apex) antes de
  B1/B2/B3.

## 📊 Estado atual

- Produção (`BotAprovacao.cs`) intacta.
- `OnFadeHarness.cs` — Fase A completa e aprovada. Guard novo pendente de recompilação.
- `OnFadeNative.cs` — escrito, **não compilado, não testado**.
- Import MNQ DEC21 no NT8 — OK, cobertura 2022→2026.

## 🔎 Auditoria do slot "MNQ DEC21" (dúvida levantada pelo Marcelo)

**Concern:** "MNQ DEC21 não pode conter set/out 2024." **Resposta: contém sim — é um slot-container, não o contrato de dez/2021.**

- O nome `MNQ 12-21` foi escolhido de PROPÓSITO em `gera_import_nt8_mnq.py`: contrato já
  vencido → o NT8 não busca feed Rithmic pra ele → o Analyzer usa 100% o dado importado, sem
  merge/override. (Slots ativos como `MNQ 12-25`/`MNQ 12-23` sofriam override do feed fino.)
- **Conteúdo real do arquivo importado** (`MNQ_NT8_import_2022_2026_CONT.zip` → `MNQ 12-21.txt`):
  - primeira barra: `20220531 200100` (2022-05-31 20:01 ET)
  - última barra: `20260831 200000` (2026-08-31 20:00 ET)
  - total: **1.505.364 barras** de 1 min
  - set/2024: 29.099 barras · out/2024: 31.740 barras
  - overnight cobrindo o pregão 17/09/2024 (16/09 20h → 17/09 09h): 840 barras ✅ tem overnight
  - preço 17/09/2024 09:30 (label): O 19788.25 / C 19795.25 — **nível real do MNQ em set/24**
    (não ~16.000 de dez/2021, não ~4.800)
  - preço 31/10/2024 09:30 (label): O 20376.00 / C 20365.25
  - `20240913 093000` = `19428.75;19428.75;19406.00;19409.00;3220` → **idêntico** ao
    `bar_ET_09:29` do dump Python da Fase A
- **Compatibilidade Fase A:** a Fase A rodou EXATAMENTE neste slot (`MNQ DEC21`) e reconciliou
  77/77 barras + 38/38 trades contra o Python. **É o slot correto e validado. B0 tem que usar
  o mesmo** — trocar quebraria a comparabilidade.
- **Risco de dado real de 2021 misturado:** nulo. O import começa em 2022-05-31; qualquer barra
  real de DEC21 (2021) estaria fora de toda janela de backtest e num preço totalmente diferente.
  E contrato vencido não puxa feed.
- **Ação de doc:** header do `OnFadeNative.cs` e este snapshot deixam explícito que `MNQ DEC21`
  ≠ contrato de dez/2021. Sem re-import.

## ⚠️ Item aberto — flatten de meio-dia (early close)

- `NyseHalfDays` + flatten 12:55 vêm do harness, que assume **fecho NYSE cash 13:00 ET**.
- O **MNQ (CME Globex)** fecha cedo **13:15 ET** nesses dias — não 13:00.
- **Não afetou a Fase A** (janela 17/09→31/10/2024 não tem meio-dia).
- **Antes do backtest 2022→2026:** confirmar se o template `CME US Index Futures ETH` modela o
  early close e alinhar `HALFDAY_FLATTEN_M` nos **dois lados** (`OnFadeNative.cs` +
  `onfade_harness.py`) com o horário real dos futuros. Datas afetadas no range: 2022-11-25,
  2022-07-03, 2023-07-03, 2023-11-24, 2024-07-03, 2024-11-29, 2024-12-24, 2025-07-03,
  2025-11-28, 2025-12-24, 2026-11-27.

## 🚧 PRÓXIMOS PASSOS

### ▶️ AMANHÃ — COMEÇAR AQUI (09/09)
1. **Claude entrega o caminho de compilação** dos 2 `.cs` (`OnFadeHarness` com o guard novo +
   `OnFadeNative` novo). Fluxo provável: link raw do GitHub (precisa `git push` da branch
   `feat/estrategia-noturna` primeiro — pedir OK do Marcelo) OU copiar direto de
   `src/` pra `Documents\NinjaTrader 8\bin\Custom\Strategies\` + F5.
2. Marcelo compila no NT8 → manda o resultado (0 erros / erros). Se `OnFadeNative` tem
   `[NinjaScriptProperty]` que quebra o Analisador, **remover + readicionar** a estratégia.
3. Com a compilação OK → rodar o **B0** (parâmetros na tabela abaixo).
4. Marcelo cola `trades_native_B0.csv` no Apple Notes → Claude compara com os 38 do harness.

**Estado ao sair hoje (08/09):**
- Commit `d6bc5d4` feito na branch `feat/estrategia-noturna`. **SEM push.**
- 3 arquivos soltos de 03/09 (`docs/apex-regras-4.0-2026-09.md`, `docs/mesas-alternativas-2026-09.md`,
  `historico/2026-09-03.md`) **fora do commit**, untracked — Marcelo decide o que fazer.
- Fase A 100% aprovada. `OnFadeNative.cs` escrito, **não compilado**.

### Antes de qualquer backtest longo — checklist obrigatório
1. Recompilar `OnFadeHarness.cs` + `OnFadeNative.cs` no NT8; **confirmar 0 erros**.
2. Confirmar valor **exato da comissão** round-turn (conta Rithmic/Apex).
3. Confirmar template de sessão = `CME US Index Futures ETH` e fuso global = Eastern.
4. Confirmar `OrderFillResolution` e que não há **dupla contagem** de comissão/slippage
   (código = 0; Analyzer = fonte única).

### B0 — teste curto de sanidade da Fase B (OBRIGATÓRIO antes do período completo)
Parâmetros EXPLÍCITOS:
| Item | Valor |
|---|---|
| Estratégia | `OnFadeNative` |
| Ativo | **`MNQ DEC21`** (o mesmo slot-container da Fase A — auditado acima) |
| Período de dados | 13/09/2024 → 01/11/2024 (= Fase A) |
| TradeWindow | 2024-09-17 → 2024-10-31 (= Fase A) |
| Deslizamento (Analyzer) | **0** |
| Comissão (Analyzer) | **nenhuma / $0** (sem template) |
| Contratos | **1** |
| Trading Hours | `CME US Index Futures ETH` (= Fase A) |
| OrderFillResolution | **Standard** (hardcoded em `OnFadeNative.cs` SetDefaults) |
| Calculate | OnBarClose (hardcoded) |
| Fuso global NT8 | Eastern (confirmado em Opções — `(UTC-05:00) Hora Oriental`) |
| Rodada | `0` → nomeia `trades_native_B0.csv` |

- Comparar `trades_native_B0.csv` contra `backtest/onfade_faseA/trades_ninjascript_slip0.csv`
  (38 trades do harness) — **conjunto de trades** (dias, direções, motivos) + os **fills reais**
  e **eventos** de `OnExecutionUpdate`, não só o número.
- Classificar cada diff: **execução nativa do NT8 (esperado)** = fill no open de B+1, preço de
  fill do stop/alvo (não o teórico), flatten ~1 barra depois, gap através do stop · vs **bug do
  código** = trade a mais/menos, direção errada, lado stopado reentrando, contagem/dia furada,
  motivo trocado.
- ⚠️ `SetStopLoss`+`SetProfitTarget` **não é prova** de que stop/alvo executaram igual ao
  harness. O B0 tem que olhar os fills e os eventos reais.

### B1/B2/B3 — econômico
- B1: Deslizamento 1t + comissão real · B2: 2t · B3: 3t.
- Período completo 2022→2026 (IS) + holdout, **só depois** de B0 fechar.
- **Go/no-go econômico = relatório do Strategy Analyzer**, conferido contra `OnExecutionUpdate`.
  Não usar o PnL do harness nem o `pnl_bruto` do CSV nativo como resultado final.

## 💡 Observações

- Deploy `.cs` pro NT8 = link raw do GitHub, Ctrl+S por cima da pasta Strategies + F5
  ([[feedback_nt8_deploy_raw_github]]). Se `OnFadeNative` tem `[NinjaScriptProperty]` novo,
  lembrar de remover+readicionar a estratégia no Analisador.
- Não repetir o padrão "propõe → backtesta uma semana → morre OOS". Critério de morte do
  OnFade tem que ser cravado ANTES do B1 (sugestão: PF > 1,15 IS **E** > 1,05 holdout, igual
  às outras candidatas).
